from itertools import islice
from typing import Any, Callable, Iterable, Sequence, TypeVar, Union
import torch as torch
import torch.distributed as dist

def maybe_all_cat(x: torch.Tensor) -> torch.Tensor:
	pass

